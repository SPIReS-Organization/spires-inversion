# Documentation build requirements for Read the Docs
# Note: We skip installing the package to avoid building C++ extensions
# The C++ module (spires_inversion._core) is mocked in conf.py via autodoc_mock_imports

# Sphinx and extensions
sphinx>=4.0
sphinx-automodapi
sphinx-rtd-theme
pydata-sphinx-theme
myst-parser
nbsphinx
sphinx-markdown-tables

# Runtime dependencies (needed for importing Python modules)
numpy
h5py
scipy
xarray
netCDF4
dask[distributed]

# spires-contract is imported at module load (spires_inversion.invert) but is
# not yet on PyPI; install the pinned tag from GitHub. Replace with a plain
# version spec once it publishes.
spires-contract @ git+https://github.com/SPIReS-Organization/spires-contract@v0.1.0

# Version detection
setuptools-scm
